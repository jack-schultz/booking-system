/**
 * Demo mode entry — placeholder until demo auth/session is implemented.
 */
export function wireDemoModeButton() {
    document.querySelectorAll('.demo-mode-btn').forEach((btn) => {
        btn.addEventListener('click', () => {
            // Demo mode not yet implemented
        });
    });
}
